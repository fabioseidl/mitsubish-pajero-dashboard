[env:native_tests]
platform = native

build_flags =
    -std=c++17
    -I../lib/core/include
    -I../projects/server/include
    -I../projects/server_emulator/include
    -DUNIT_TEST

test_framework = unity
test_filter    = host/*
